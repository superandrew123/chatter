<!-- <br>
<div class="col-md-3">
	<h4>Followers: <%= @followers_count%></h4>
	<h4>Following: <%= @followings_count%></h4>
</div> -->
<body>
<h2 class="handle">@<%= @user.handle %></h2>
<h2 class="username"><%= "#{@user.name}" if @user.name.length > 0 %></h2>
<p>&nbsp</p>
<div class="circular"></div>
<div class="col-md-6">
<p>&nbsp</p>
<table class="table" width="100%" cellspacing="0">
	<tr class="follow_data">
		<td><%= @followers_count%></td>
		<td><%= @followings_count%></td>
	</tr>
		<tr>
		<td>followers</td>
		<td>follwing</td>
	</tr>
</table>
	<% if current_user %>
		<% if @following_status %>
			<%= link_to "Unfollow", @follower, method: :delete %><br><br>
		<% elsif !@following_status %>
			<%= link_to "Follow", new_follower_path(@user) if @user != current_user %><br><br>
		<% end %>
	<% else %>
		<a href="/users/sign_up">Sign-up</a> or <a href="/users/sign_in">Log-in</a> to follow!<br><br>
	<% end %>

	<% @user.posts.each do |post| %>
		<p>Post: <%= post.post_content %></p><br>
	<% end %>

</div>
</body>
