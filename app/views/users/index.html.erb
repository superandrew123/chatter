<div class="col-md-3"></div>
<div class="col-md-6">
	<p>You searched for <%= @q.gsub("%","") %>, here are your results:</p><br>
	<% @users.each do |user| %>
		<%= link_to "#{user.handle}", user_path(user) %><br>
	<% end %>
</div>