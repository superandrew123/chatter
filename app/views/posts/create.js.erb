// select the div partial will go inside of the element and prepend so it shows the most recent
$("#posts").prepend("<%= j render partial:"show_post", locals:{post: @post} %>");
$(".countdown").each(function(){
	$(this).countdown({
		date: $(this).children('.expiredate').attr("value"),
		format: "on"
	});
})
$("#post_post_content").val("");

$(".commentButton").on("click", changeReply);